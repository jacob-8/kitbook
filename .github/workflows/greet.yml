name: Greet

on: pull_request

jobs:
  greet:
    steps:
      - uses: ./packages/visual-regression-action
        id: hello
        with:
          who-to-greet: Jacob

      - name: Get the output time
        run: echo "The time was ${{ steps.hello.outputs.time }}"
