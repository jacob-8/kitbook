<script>
  import { View } from 'svench';
  import { longpress } from './longpress';
</script>

<View knobs={{ duration: '200-2000;1000' }} let:knobs={{ duration }} let:action>
  <button
    class="p-3 hover:bg-gray-200"
    type="button"
    use:longpress={duration}
    on:longpress={action('longpress')}
    on:shortpress={action('shortpress')}>Press/tap more or less than {duration}</button>
</View>
