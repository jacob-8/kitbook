<script lang="ts">
  import { Story } from 'kitbook';
</script>

# Sandbox

This is a page which does not inherit the (main) group layout so it can display components and stories in isolation when loaded via an iframe.

To let the following story be shown, we must now begin matching properly in `+page.ts` (next commit)

<Story>
  Let's use a real story now!!
</Story>