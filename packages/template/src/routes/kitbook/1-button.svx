<script lang="ts">
  import Button from '$lib/Button.svelte';
  import { Story } from 'kitbook';
</script>

# Button

<Story knobs={{route: '/kitbook'}} let:props={{route}}>
  <Button {route}>Kitbook Home</Button>
</Story>

Try changing the knob to `/kitbook/0-file-ordering` and click it to navigate to that page. Yes, the button is a bit *ugly*, but it's just for demo purposes...